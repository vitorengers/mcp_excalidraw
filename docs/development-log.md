# Development log

What this fork has landed, and what was decided while landing it. Newest first.

This is the development half of the board. The other half — `1 · How the pieces fit`,
`2 · The blocks on the canvas`, `3 · Try it` — is the **Project structure** map: what the tool
is made of, right now, undated. This one is the record of how it got that way, and every row
carries the date it landed on.

It replaces `delivered.md` and `open-work.md`, which said the same thing about a smaller
repository and then stopped. `delivered.md` still opened with "Issues #1–#11, all closed" when
`main` was past #83; `open-work.md` still called #20 and #21 the open work. A document that has
to be rewritten to stay true gets rewritten once and then never again — a log that is only ever
appended to does not have that failure mode. What is still *ahead* is not here: that is
[whats-next.md](whats-next.md), and it is forward-looking on purpose.

## Adding an entry

One row per merged pull request, at the top of the table, before the pull request is merged:

| Date | Issue | PR | What was decided |
|---|---|---|---|
| 2026-07-28 | #153 | #170 | The terminal overlay is clipped to the canvas area, and the page chrome is put on a layer above it. The card is drawn at its block's bounds in viewport coordinates with no clamp, which is right — the card **is** the shape, so it pans and zooms with the board — but nothing stopped it at the edge: the canvas wrapper declared no `overflow`, and `.workspace-tabs` and `.header` are `static` and unpositioned, so a card carrying any `z-index` at all painted over them. A block panned above the top drew its title band and tab strip across the chrome, hiding the connection pill, `Sync to Backend` and `Clear Canvas` and — the card's body takes the pointer — taking their clicks with them. **Clipping rather than sliding it back on screen.** Sliding is what the documentation card does, and it is why that card never showed this: `placeCard` clamps it into the canvas area before it is drawn. This one is pinned to its shape on purpose, and a card that slid back would be a card coming loose from its block; clipped, it stops exactly where Excalidraw already clips the rectangle under it. **The chrome gets a layer as well as the clip**, because one `overflow` declaration is a thin thing for `Clear Canvas` to depend on. **The ladder is named once**, on `.app`: `--board-z-overlay: 5`, `--board-z-chrome: 10`, `--board-z-dialog: 50`. It was a bare `5` in two stylesheets and a `50` in a third, none of which said what they were relative to, which is how nobody noticed the two strips they had to stay under were on no layer at all. **The overlays stay above Excalidraw's own layer UI**, which the issue asked to have settled and nothing here had decided. The other reading is defensible — the card stands for a shape and a shape goes under the tool islands — but the documentation card shares the layer and is opened by *selecting* a shape, which is exactly when Excalidraw's properties island is on screen beside it: under the islands that panel's own buttons become the thing nobody can click, on every shape near the left edge. A terminal over an island can be panned off it; a panel under one cannot be recovered. `check-terminal-overlay-layer-browser.mjs` asks `document.elementFromPoint`, not a bounding rect, because **a clipped card reports the same box it always did** — a check that measured rects would have been green before the fix and after it. It failed nine cases against the old code, with its #112 section passing, which is what says the reproduction was the layering rather than the check. |
| 2026-07-28 | #164 | #169 | Case 10 of `check-issue-comment.mjs` reads the action row's order **out of the row**, having compared `indexOf` positions over the whole of `DocsPanel.tsx`. The first occurrence of `Implement / Fix` in that text is not the button: it is prose in the hint an interrupted run shows — real JSX text, so stripping `{/* … */}` comments left it — declared hundreds of lines above the row, and the observation button was being measured against a sentence that merely names the other one. **Nothing about the row was ever wrong**, which is what makes this worth an entry rather than a one-line fix: the case was true when written and stopped being true when a hint naming a button was added above it, so the check went red on a change that could not have broken what the check is about, and stayed red long enough to become background noise. A position compared over a whole file cannot tell a button from a sentence about one. **Depth counting rather than the shorter regex.** The neighbouring container case already matched `element-docs__actions[\s\S]*?Implement \/ Fix`, and reusing it was the obvious move and the wrong one: a region that *ends* at `Implement / Fix` cannot express "Add observations comes first" — the ordering case would degenerate into a second copy of the container case, and a genuine swap would surface as a row that lost a button rather than as a row in the wrong order. `actionRow()` counts `<div`/`</div>` depth from the opening tag instead, so the whole row is in hand and a swap fails the ordering case and only the ordering case. **Proved by swapping the two buttons in the panel** before the fix went in, per `CLAUDE.md`, and confirming the container case stayed green while the ordering case went red — the failure had to be for the ordering reason, since the old case would also have gone red for the wrong one. **The browser was the other option and was declined**: `check-issue-recreate-browser.mjs` already reads `.element-docs__action` out of the DOM and would assert the rendered order, but `check-issue-comment.mjs` is not a browser check and giving it a headless Chrome for one assertion is a larger change than a misreading substring calls for. Nothing under `frontend/` or `src/` is touched, so there is nothing to look at in a browser. Log only: a check reading a different substring adds no file, route, block kind or feature. |
| 2026-07-28 | #166 | #168 | A branch already checked out somewhere else is detected **before** `git worktree add` runs, and the refusal says which checkout holds it and how to free it. The reported failure was git's own, and git was right to refuse — two checkouts of one branch diverge with nothing to reconcile them — but its wording is `'issue-155' is already used by worktree at 'C:/…/mcp_excalidraw'`, and that path is the **project itself**, listed as a worktree because `git worktree list` lists it as one. A reader looking at the directory their project lives in, described as a worktree, has nothing to act on; every later click failed the same way and nothing on the board suggested a repository needed a `git switch`. **`show-ref` was the wrong question all along**: it answers whether a branch exists and never where it is checked out, so the one state that makes creation impossible was the one state nothing looked at. `checkoutHolding` reads `git worktree list --porcelain`, which carries both halves — a `worktree <path>` line and the `branch refs/heads/<name>` line under it. **The two holders get different sentences**, because they are different situations: the project itself gets the `git -C "<path>" switch <default>` that frees it, with the default branch resolved through `baseRef` rather than guessed, and a note that nothing is lost; another worktree gets told the run may already be set up there. **The check runs after the reuse check, deliberately** — the checkout holding this issue's branch is usually this issue's own worktree, and reuse is what makes a retry after an interrupted run land in the work the previous attempt left, which is #126's whole point. **Nothing moves anybody's checkout.** Switching a clean project back was considered and rejected: the board cannot know who is standing in that directory — an editor open on those files, a shell mid-task — and a branch that changes underneath any of them is a worse failure than this one, because it is silent. **How the state is reached is not this repository's to fix, and is worth writing down.** The board's terminal opens a shell at the workspace root, which *is* the project's checkout, so a coding session started there works on the repository directly; one that switched to `issue-155` and did not switch back left it that way, and the reflog shows the same shape earlier the same night on `issue-117`, that one switching back. An implement agent is not a suspect: `worktreeSection` already tells it not to switch its checkout, and that instruction exists only when a worktree was made — which here it never was. `check-worktree-branch-held.mjs` is new, self-contained, and was run against `main` first, red on the two cases that are the whole of the defect: the wording and the missing command. Its third and fourth sections are the ones a too-eager detector would break — a branch nothing holds, and this issue's own worktree — and both were green before and after. Log only: a detection and a message add no file, route, block kind or feature. |
| 2026-07-28 | #165 | #167 | An agent's command is resolved **per workspace**, so a project inside a WSL distro can be given the command that distro knows. `EXCALIDRAW_ISSUE_AGENT` and `EXCALIDRAW_IMPLEMENT_AGENT` were one global string and `buildAgentCommand` decided only *where* to run it, which cannot work: a command is a path, and `C:/Users/x/.local/bin/claude.exe` is `No such file or directory` inside a distro exactly as `/home/x/.local/bin/claude` is nowhere on the host. Every research run on the one WSL board answered `Agent exited with code 127`, and #157's own entry had already recorded this as found and not folded in. **Nothing was missing inside the distro** — `claude`, `gh` authenticated, and the checkout were all there; only the string being sent was from the other machine. **A config field was never on the table.** `workspaces.ts:30-34` argues that a project file which could supply a command would let anyone with write access to a registered repository start an unattended agent on a board where nobody allowed one, so `EXCALIDRAW_ISSUE_AGENT_WSL` and `EXCALIDRAW_IMPLEMENT_AGENT_WSL` are granted by the operator like the two they sit beside; discovering `claude` inside the distro would have been worse still, granting from nothing at all. **The fallback is deliberate and one-directional**: a WSL workspace with no `_WSL` variable still runs the global command, because one written without an absolute path — `claude -p …` — resolves in either environment and refusing it would break a working board to fix a broken one; a native workspace never reads the `_WSL` half, since a grant for the distro says nothing about the host. **Being enabled became a fact about the workspace rather than the server.** The early `if (!COMMAND)` guards now ask only whether *any* command exists, and each route resolves again once it knows which project it is in — the implement one after the slot is claimed, so its refusal goes through `releaseSlot` like every other late refusal and a workspace with no command cannot shrink the cap. **Exit 127 says which side of the boundary it happened on.** It is the one exit code this process understands better than the shell does, so a WSL run appends the distro's name and the variable to set; a native run is untouched, because there the shell's own message is already about the machine the reader is on. `check-wsl-agent-command.mjs` is new and went **12 cases red** against the old code, the behavioural one reproducing the reported error verbatim from a real distro. It does not stop at the missing export: the resolver is stubbed so every case reports, since the run in a real distro is the one that shows what the reader actually sees. Log only — two environment variables and a resolver add no file, route, block kind or feature the map names. Found on the way and worth its own issue: `git worktree remove --force` follows a junction, so a `node_modules` mirrored that way is emptied through the link when the worktree goes; the server's own `linkDependencies` uses hard links for this reason and a hand-made junction does not. |
| 2026-07-28 | #128 | #149 | An implementation runs in a terminal session the board can show. It spawned a private child on three pipes and read what it had written once it exited — `stdout += chunk`, one read on close — so a board could say a run was `running` and nothing else; the terminal was built beside it and nothing ever joined the two, which `docs/terminal.md` recorded three times and #51 deferred on purpose. **`runAgent` takes a `host`, and null is the important half.** Given one the run happens in a session opened in its worktree and owned by the issue, and the tab appears with nobody clicking for it; given none — no terminal on this board, no PTY, all eight tabs taken, a host that threw — it falls through to the private child it always used. That is what keeps the two opt-ins independent and stops a 409 from the cap ever being what refuses an implementation. **The session is on pipes, and that is measured rather than preferred**: probing ConPTY with a Node child reading stdin, neither `^Z` nor `^D` produced `end` — it read until it timed out — so a `claude -p` handed several hundred words of prompt through a pseudoterminal would wait forever for a prompt that never finished arriving. The constructor therefore ignores the binding whenever `input` is set rather than leaving the trap for a caller, and `mode` says `pipe` so no block claims otherwise. That settles the issue's blocking open question the other way round from how it was posed: because the process inside the tab **is** the process that ran before, in the same checkout, reading the same prompt, there is no repainting screen to scrape — one `agentOutcome` decides `done` with a pull request and `failed` with an error for a watched run and an unwatched one alike. The price is a tab that is read-only in substance, and `write()` swallows a keystroke rather than echoing one that would read as though the agent had heard it. The operator's `EXCALIDRAW_IMPLEMENT_AGENT` is not rewritten — no flag, no appended argument — which is the rule `agent-usage.ts` already keeps. Two more answered: #122 had already landed as #139, so the marker question was moot; and the tab **does not come to the front**, because four unattended runs at the default concurrency, plus a queue that starts them with nobody watching, would take the view away from a shell somebody is typing in. `POST /api/terminal` gains an optional `command` and `cwd` so the server's own entrance is one a person can also reach — a facility only the server can use is one nothing can check by hand — with all three guards untouched, since a shell is already a thing you type commands and `cd` into. `check-implement-terminal.mjs` went 14 cases red against the old code and caught something on the way: a board running on this machine exports `EXCALIDRAW_TERMINAL=1`, so the case about a board with **no** terminal was quietly being run against one that had it. Structure map: the Terminal card says a run opens one in its worktree. Left for its own issue and written into `whats-next.md`: the **issue** agent has the same gap and the seam already exists — `runIssueAgent` simply does not pass a `host`. |

- **Date** — ISO 8601, the day it lands.
- **Issue** — the issue the branch was cut for, or `—` for the handful of early changes that
  went straight to a pull request.
- **PR** — the pull request. It is written before the merge, so the log runs one entry ahead of
  `git log`; `scripts/check-board-map.mjs` only fails the other way round, on a merge with no
  entry.
- **What was decided** — the decision, not the diff. `git log` already has the diff.

A change that also moves the architecture — a file, a route, a block kind or a feature added or
removed — updates the **Project structure** map on the board as well. `CLAUDE.md` is where that
rule lives.

## Entries

| Date | Issue | PR | What was decided |
|---|---|---|---|
| 2026-07-28 | #155 | #157 | A WSL workspace's command is `--exec`-ed rather than handed to the distro's default shell, so it is parsed once instead of twice. `wsl.exe <command>` runs what follows through that default shell, which meant `-- bash -lc "<command>"` had a shell in front of the shell that was asked for. **Quoting survived it, and that is why nobody saw it**: the argument count was right and every argument arrived whole, so the command looked correct at every place anyone would have inspected it. What did not survive was every `$name` inside the quotes, *single quotes included* — by the time the inner shell saw the string the variable was already gone rather than quoted, so there was nothing left for the quoting to protect. **It took the project board mirror out entirely on any WSL workspace, for as long as one has existed.** `readProjectBoard` sends a parameterised query, so `$login`, `$number` and `$field` reached GitHub as nothing and every twenty-second poll answered `Expected VAR_SIGN, actual: COLON (":") at [1, 7]` — the colon that replaced `$login`, six characters into `query(`. The comment at `project-board.ts:85-88` had reasoned the query safe because it holds neither a quote nor a line break, which is sound as far as it goes: `$` is the character it does not cover, and a second shell is what makes `$` matter. That the three boards which have ever shown a working mirror are all native Windows is the whole of why this survived a hundred and forty merges. **The scope is wider than the symptom and the fix is at the shared choke point**, not at the query: every `gh` call and every agent run in a WSL workspace goes through `buildAgentCommand`, and the query is merely the one command line here that always contains a variable. A native workspace is untouched, tokenised and spawned with no shell anywhere, and that is asserted as its own case rather than assumed. `check-wsl-command-quoting.mjs` is new and was run against the old code first, red on two cases — the behavioural one reading back `q(: String!) { x }` where `q($farol_not_a_variable: String!) { x }` had been sent, and the structural one on the argument list. **The behavioural case needs a real `wsl.exe` and skips where there is none**, loudly rather than quietly, which is why the same decision is also asserted structurally: a check whose only real assertion skips on the machine that runs CI is a check that stops being run. The variable it prints is named so that no environment can define it and make a broken build pass. Found and not folded in, for an issue of its own: a WSL workspace still cannot run either agent, because `EXCALIDRAW_ISSUE_AGENT` and `EXCALIDRAW_IMPLEMENT_AGENT` are one global command for every workspace and a Windows absolute path to `claude.exe` is `No such file or directory` inside the distro. Log only: a check script and a flag on a spawn add no file, route, block kind or feature. |
| 2026-07-28 | #144 | #160 | The terminal opens half again as big, its tab strip is drawn half again as big, and the bar along the bottom is gone. **The size was never argued.** `TERMINAL_SIZE` was the one constant in `terminal-block.ts` whose comment said only "it is resizable from there" where every neighbour argues its value at length, and 760 x 480 is about a hundred columns by twenty rows — enough for a prompt, not for the agent transcript this block exists to hold. 1140 x 720 is half again in **each direction** rather than by area; the area reading gives 931 x 588, and a request about how big a window looks is a request about its edges, which is how #110 read its own 2.5. Only a fresh block reads the constant, so nothing already on a board moves. **The strip scales on one lever**, `--terminal-tab-scale` on the card and read by the tab row alone, the shape `--workspace-tab-scale` took in #110: every length in the row is `em` off the row own font-size, so one multiplier carries the chips, their close marks, the three controls, the gaps and the inset. **The header is deliberately not scaled** — since #112 that band is the whole of what selects and drags the shape, and it is the budget #112 was defending; the row around the chips stays `pointer-events: none` and only the chips and controls take a pointer, so nothing about that trade moved. The consequence the issue predicted stands: the tab row is now taller than the header over it. One thing had to change shape rather than size. The chip outline was a `1px` border, the only length in the row not in `em`, and multiplying it does not help — **a browser snaps a border to whole device pixels**, so `calc(1px * 1.5)` is laid out as `1px` and the chip grows by `1.5 x em + 2px`, measuring 1.446x. It is an inset `box-shadow` now: no layout contribution at all, so the chip is `em` and nothing else. **The bar goes, band and all.** Two of its three sentences narrated where the pointer already was, to a reader who had to have clicked the block to be reading them. The third is not narration — a shell that has gone leaves a block with no way back written down anywhere — so it is kept as `.terminal-card__ended`, drawn **over the screen** rather than in a row of its own: a row is chrome, and chrome is what `terminalGrid()` subtracts, so a band that appeared when a shell exited would re-grid the block at the moment its shell died. It takes no pointer events, so the transcript underneath stays selectable and the click that focuses the screen still lands on the screen. The four open questions are answered rather than deferred: the whole bar rather than its sentence; the notice on the screen rather than in the chip `title`, which is a tooltip and not "on the block", or printed into the emulator, which is the shell transcript; **no bottom band at low zoom**, because the claim #115 argued for is that *a* band crosses the block where nothing can be read and the header is the band that also has a second job, with `check-terminal-paper-browser.mjs` green on it alone at zoom 0.15; and the strip alone rather than the whole frame. **`TERMINAL_CHROME.height` was re-measured, not re-derived.** #144 moved the frame in both directions at once, which is the coupling #104 is a monument to and which neither `tsc` nor `vite build` can see. At zoom 1 and the default font `.terminal-card__body` came back 662.3px inside a 720 block, so the frame is 57.7 and the constant is 64 — rounded **up**, the direction #104 is safe in, since too much chrome costs a row at the bottom and too little costs rows nobody can reach. `check-terminal-rows-browser.mjs` is green at 8, 13 and 24 on both axes. `check-terminal-size-browser.mjs` is new, written first, and was **red on ten cases** against the old code: the block at 760 x 480, `.terminal-card__prompt` present, all four controls at 1.000x with the scale forced to 1, and no home for the exited notice at all. It measures the render rather than the file, and it measures the strip *against itself* with `--terminal-tab-scale` forced back to 1 — the strip as it was, in the same browser at the same zoom and reader font size, which is a fairer comparison than a number copied out of another run and the same one-lever question `check-workspace-tabs-scale.mjs` asks. Nine checks that clicked the bar now click `.terminal-card__body`; two of them place their own viewport before the corner drag, because a block half again as big puts that corner and its 200 x 140 drag past the edge of a 1500 x 950 window — and one of those, `-tabs-` case 8, **was already failing on `main` for that reason**, which #112 entry recorded as found and not folded in. Merged in mid-flight, twice. #149 puts a run label on an owned tab chip, which is wider and not taller, so the strip arithmetic is untouched. #158 gives the palette a theme and rewrote the same three files around the bands, and the reconciliation is the substantive one: its prose says the identity at low zoom is carried by *two* strips that invert with the theme, and this branch removes one of them — so the surviving claim is stated as the header band alone, in the stylesheet, in `terminal-palette.ts`, in `terminal.md` and in `check-terminal-paper-browser.mjs`, whose `bandCase` now runs on the header in **both** themes and is green in both. `bandAccent` goes with the strip: it was a second green added because an accent legible on paper is 2:1 on a band, and the caret at the bottom of the frame was the one thing that read it. Its `TerminalScreen` conflict was the two props each side removed and added, `theme` kept and `onFocusChange` gone. what did not merge cleanly on its own is the sentence counting the check list in `terminal.md`, where #149 took it 13 to 15 and this branch took it 13 to 14 and git merged two bumps into one, leaving a sixteen-item list claiming fourteen — the same trap #122 entry recorded, corrected in a commit of its own. #124 turned out not to need the correction the issue asked for: it landed as #142 and detaches to the left **by rule** rather than by a number derived from `TERMINAL_SIZE.width`, so widening the default leaves its arithmetic alone, and its check is green at 1140. Log only: no file, route, block kind or feature the map names moved — the map card for this block describes what the block is and never named the bar, and a check script adds none of them. |
| 2026-07-28 | #152 | #163 | A shape standing for an issue **nothing has been started on** offers **Recreate with observations**: a box, and an agent that investigates the repository again and rewrites the issue. Until now a created issue could only be *appended* to — #53's **Add observations** posts a comment, and a comment cannot correct a body, so a first investigation that went the wrong way stayed wrong and the next reader of it was an unattended coding agent. **Design A: the issue is rewritten, not replaced.** The observation said "Recreate", and the issue left A-or-B open; rewriting in place keeps the number, the card, the column, the comments and everything the server keys on the URL valid, and nothing has been built against the issue yet, so there is nothing to preserve by leaving a superseded copy behind. **Strict Todo**, the fourth open question, decided narrow: the workspace's own `projectTodoColumn`, matched trimmed and case-insensitively, which is where a finished research run already puts an issue. Past it, rewriting a body would change the specification behind a running agent's back with no way to tell it — the same hazard a mid-run comment has, except a comment cannot silently replace what the agent read. **The observations are posted as a comment first** (question two, decided yes): a body that changed with nothing on the issue explaining why is unreviewable, and a run that dies having posted it leaves the reader where **Add observations** would have rather than losing what they typed. **The title may be rewritten too** (question three, yes) — it is what a card is labelled with, so a new one reaches the board on the next poll. **Reference images stay out** (question five): the file store is in memory and the block holding `issueImages` is gone, so there are none to pass; worth its own issue. **The run record is this issue's job after all** (question six): a card has no `issueState` to hold `running`, so the state lives in memory against the workspace and URL and the panel polls `GET /api/issue/recreate?url=`, forgotten on restart because a `gh issue edit` either happened or did not. **`POST /api/issue-block/:id` was not relaxed** — it still 409s on any block with an `issueUrl`; recreating is a route of its own because it opens no issue. **The gate is a mark, not a label**: `readProjectBoard` carries the Todo column name on the board, `layoutBoard` stamps `customData.inTodo` on the cards in it, and that reaches `IssueTargetData.recreatable` — reading the column out of a header's text would be reading a string that wraps and carries a count. An agent that answers with a *different* issue URL is recorded as failed: that is what a revise that opened a second issue looks like. `scripts/check-issue-recreate.mjs` (52 cases red against the old code) and `scripts/check-issue-recreate-browser.mjs` (3 red) are both new and were run against `main` first. |
| 2026-07-28 | #151 | #161 | The enforced documentation set was `docs/*.md`, built by `check-board-map.mjs` from `git ls-files docs`, and inside that boundary everything was healthy. Everything that had drifted was outside it: `README.md` was still the upstream project's front page, `docs/rest-api.md` said `27 routes` against 50 in `src/server.ts`, four documents cited a start script that has never been in `git ls-files`, fifteen of twenty-two documents were linked from nothing, `docs/whats-next.md` asked for a browser test that shipped fifty merges ago, and `docs/` was scanned by no check at all — `isScanned` in `check-english-only.mjs` admitted a file only under one of four directories **or** with no slash in its path, so `docs/x.md` matched neither arm and 268 KB of prose, the largest body of writing in the repository, went unread. **The decision is that a claim a machine can check gets checked**, and the counts are the clearest case: `check-docs-counts.mjs` reads the routes, tools and commands out of `src/` and fails on any number asserted anywhere that disagrees, so `27` against `50` goes red instead of being noted in this log and left. `check-docs-index.mjs` holds an index every document is reachable from, and every path the prose names existing; `check-readme.mjs` holds the front page to being about this fork, deriving the block kinds from the exported `*_KIND` constants and the section keys from the board itself, so a new kind or a renamed section fails here rather than quietly missing; `check-docs-encoding.mjs` reads bytes rather than words — strict UTF-8, no U+FFFD, no BOM, and no cp1252 double-decode pair. That last one is green on a clean tree, which is indistinguishable from a detector that cannot detect, so it carries its own fixtures and was also run against a deliberately corrupted copy of the board. **This log is exempt from the count and path rules**: it is the record of what was true then, and rewriting history to keep a check green is the wrong direction. **The one defect the issue wrote off turned out to be the one in the screenshot the issue was opened about.** A row of cards drawing as empty boxes was cleared as already fixed, because all 26 rectangles carry a title and a body — true of the file, false of the canvas. Excalidraw treats fractional indices as valid only while they *increase along the array*; `export-board.mjs` wrote the board in `id` order on the reasoning that paint order lives in `index` rather than in array position, so Excalidraw read the indices as broken, regenerated them from the array order, and painted 20 strings underneath the opaque cards they label. The export sorts by `index` now, ties broken by `id`, compared as plain strings because `localeCompare` orders `aa` and `aA` the other way and would reintroduce the same inversion under another name. It cost nothing that mattered: `index` only changes when something is genuinely restacked. `check-board-z-order.mjs` was red on HEAD's board on 37 inversions and 20 buried strings. Folding it in was a deliberate override of the issue's scope, on the grounds that the stated reason for excluding it was factually wrong and that the definition of done requires the rebuilt board to be *looked at in a browser*. **The run procedure is a procedure, not a script**: every value in it that has to be a path on one machine is the operator's, and `HANDOFF.md` — the only place it existed — is untracked, so a script would have meant inventing behaviour rather than recording it. `docs/running.md` carries all fifteen `EXCALIDRAW_*` variables with the defaults read out of `src/`, and 3737 against 3838 is settled in `CLAUDE.md` as two servers rather than a contradiction. On the board: no `customData.kind = "issue"` element remains — a finished request from #25 had been sitting in the Project structure section as architecture — and the implementation pipeline finally has a row in section 1. |
| 2026-07-28 | #147 | #158 | The terminal's palette takes a **theme**. #115 decided paper in *both* themes on the reasoning that following the canvas would mean two palettes to keep legible instead of one; that was never on offer, because dark mode here is `filter: invert(93%) hue-rotate(180deg)` on Excalidraw's canvas and the overlay is a `div` sibling of it. So the block's `#faf6ee` was painted near-black for free and the card painted a literal `#faf6ee` over it — one palette *declared* and two *painted*. Today's values are the light branch unchanged; the dark one is Catppuccin Mocha with every slot lifted until it clears 3:1 on its own surface, the mirror of Latte darkened for paper, since neither survives as shipped (Mocha's own `black` is 1.8:1 there, Latte's yellow 2.4:1 on paper). **The shape's fill does not move**, which is the answer to the issue's first open question and neither of the two it offered: it is scene data, so a fill that followed the reader's `localStorage` theme would make every toggle a change to the board and two readers with different themes two boards, and it would be the one block on this canvas opting out of the filter every other block is drawn through. The dark palette's *surface* is therefore defined as the colour that fill comes out — `#1d1912`, measured off a render, warm rather than Mocha's cool `#1e1e2e`, which is the single place the dark palette leaves Catppuccin and it leaves towards the paper it is the night side of. **The band inverts rather than staying dark**: it is the ink colour in both, so on night it is a light strip on a dark card, and it has to be, since the board behind it is dark too and a dark band on a dark card at zoom 0.15 is the exact failure the band was invented to prevent. The whole set of custom properties is swapped in TS rather than by a `[data-theme]` rule — they are inline styles, which a rule cannot outrank — and the three white washes hardcoded in `TerminalPanel.css` became variables, per #121's trap that a palette declared in part is worse than one not declared at all. The live emulator is **re-themed**, not rebuilt: disposing it replays the transcript into a fresh parser, which loses anything on the alternate screen. `check-terminal-paper-browser.mjs` is parameterised by theme, **pins** it rather than inheriting `prefers-color-scheme`, and its surface case is a **rendered-pixel** comparison — the same two coordinates with the overlay over them and with it out of the way — because the string equality it replaces compared `#faf6ee` with `#faf6ee` and passed while the screen showed a bright card in a dark ring. Eight cases red first. The issue's second open question is answered and **not** fixed here: the banner is Claude Code's hint chip drawn as ANSI `black` on `brightCyan`, 1.11:1 on paper and 2.15:1 on night — a slot used as a *background*, which nothing checks, and illegible in light mode since #115. Its own issue, #159. |
| 2026-07-28 | #135 | #150 | The `+` on `My Notes` makes a block only when there is no **unwritten** one already in the column; where there is, that block is selected and no second one is made. The `+` is a shape rather than a button, so the click on it is the selection landing on it, and the handler handed the selection straight back to the block it had just made — which deselects the `+` and re-arms it for the very next press. Nothing counted what was already there either, so a double click left two empty blocks and five presses left five, each to be deleted by hand. **Unwritten is read off the element, not off a flag**: the label still says what the library ships, compared on one line so a narrower column's wrapping does not read as an edit, and the block carries no `issueUrl` and no `issueState` — a block a run has claimed is somebody's even though nobody typed into it. A flag would be a second copy of the answer that every place able to edit the text would have to remember to clear. Only unwritten blocks are capped, so the `+` still owes a fresh block to a reader who has written the last one down. Every draft on the canvas is considered rather than only the ones stamped with this column: the stamp is vestigial since #117, so reading it would let a block stranded by an old ordering hide from the cap. **The undo hypothesis in the issue does not reproduce, and nothing was written for it** — `Ctrl+Z` restores the selection to what it was *before* the press rather than onto the `+`, so nothing fires, and that case passes against the old code as well as the new. It is kept as a regression guard rather than answered with a pointer-arming mechanism no failing case demanded. `scripts/check-notes-add-once-browser.mjs` is new and failed nine cases against the old code. `check-board-drafts-browser.mjs` asked for its second block by clicking twice with nothing typed, which is the gesture this refuses, so that case was rewritten to write the first observation down first rather than deleted. Left out and worth its own issue: the self-contained browser checks inherit `EXCALIDRAW_TERMINAL` from whoever runs them, and with it set the terminal block's xterm overlay sits over the mirror and swallows clicks aimed at the `+` — the new check deletes it from its own environment, the older ones are a flake waiting on the reader's shell. |
| 2026-07-28 | #140 | #148 | The run's `out` figure is split, `28.4k out (12.1k thinking)`, and half the issue's premise was wrong in a way worth recording: **reasoning was never missing from `out`**. Those tokens are billed as output and have always been inside that number, so nothing was being dropped — what the board could not show was the decomposition, which is why this is a bracketed share and not a third `·` segment to be added to the other two. **The field the issue named does not exist in this stream.** `output_tokens_details.thinking_tokens` is the Messages API's own breakdown, and the issue flagged as unverified whether the CLI forwards it; run before any parsing was written, `claude -p --output-format stream-json --verbose` (2.1.220) sends it **nowhere** — not on an assistant message, not on `result` — so implementing the scope as written would have shipped a figure that is always absent. What the CLI sends instead is `{"type":"system","subtype":"thinking_tokens","estimated_tokens":…,"estimated_tokens_delta":…}`, and two observed properties decide the code: `estimated_tokens` **restarts at every assistant turn** (50 → 150 → 173, then 50 → 129), so it is the turn's total and reading it at face value makes a run's reasoning go *backwards* — the deltas are what accumulate; and `result` says nothing about reasoning at all, so unlike input and output the figure has no settled counterpart and is kept **beside** the settled totals rather than inside them, which answers the issue's second open question the way its first one forced. Fold it in and the split shows for the length of the run and vanishes the moment the run is finished and worth reading. Both shapes are read anyway — a breakdown inside `usage` wins, the summed estimate fills the silence — because `EXCALIDRAW_IMPLEMENT_AGENT` is somebody else's command line and need not be Claude Code; it is a choice rather than a merge, which is also what stops anything being counted twice. **Null, not zero**, when neither arrives: an agent that reports no breakdown has not claimed its reasoning was nothing. The subtle one is the throttle — reasoning moves on events of its own, so `report` compares all three figures now; watching only in and out would hold a new figure back until the next message happened to settle, and the check makes that its own case by releasing a thinking event with nothing else changing. Wording, the third open question: brackets after `out`. `scripts/check-implement-progress.mjs` was run against the old code first and went 14 cases red across five sections. **Seen in a browser against a real CLI run**, not only the stub: `0:24 · 87k in · 72 out (93 thinking)`. That reads impossible and is the streamed `output_tokens` lagging — reasoning arrives complete while output is still catching up — so it is documented rather than clamped to `out`, since the smaller number is the stale one and capping the honest figure to it makes both wrong instead of one late. Noted for an issue of its own: under `stream-json` the CLI emits only subagent `tool_use` and `tool_result` by default, so a run that spawns subagents under-reports all three figures until `result` lands, and `result` settles two of them; `--forward-subagent-text` is where that starts. Log only: no file, route, block kind or feature added or removed. |
| 2026-07-28 | #132 | #145 | The block being typed into is kept out of the mirror's redraw, and the redraw it did not need is not issued at all. `relayoutForDrafts` runs from Excalidraw's `onChange`, and a container grows to fit its bound label, so a keystroke that changed a draft's height put a whole-scene `updateScene` through on every keystroke. The `frozen` guard held the edited block in its slot but left it in the array handed to `convertElementsPreservingImageProps`, which runs everything through `convertToExcalidrawElements`: the container is deep-cloned and its label rebuilt through `newTextElement`, re-measured and shifted by the alignment offsets. **The guard protected the block's slot and not its content.** So the frozen container and its label are now put back into the converted array by id, keeping their place in it — they came out of the scene and are already Excalidraw elements, so the conversion had nothing to do to them. **And the block's own height comes out of the signature the redraw is skipped on**, because that block is the one the pass never moves; where the blocks *below* it go stays in and still moves them, which is #55/#62 and is asserted directly. With nothing under it, typing into a block now writes no scene at all. **Object identity is what the new check measures**, and it is the instrument the old one lacked: Excalidraw replaces the *text* element on every keystroke — that is how it writes the text — but leaves the rest of the scene alone and grows a container by mutating it in place, so a card, a header or an edited container that is a different object after a keystroke was handed over by a whole-scene write, and nothing else makes one mid-keystroke. `check-board-drafts-typing-browser.mjs` reads the block back rather than the layout and failed exactly those two cases against the old code; `check-board-drafts-browser.mjs` types into a block too but asserts only geometry and never reads the string back, which is why this had passed unnoticed since #55. **Both of the issue's hypotheses for the vanishing word failed to reproduce, and that is worth keeping rather than quietly dropping.** `originalText` matched what had been typed, character for character, at every one of ~440 sampled keystrokes *on the old code* — and it cannot do otherwise, since `textWysiwyg` sets `editable.value` once at open and `handleTextWysiwyg`'s `updateElement` rebuilds `originalText` from the textarea by id, so a replaced element object cannot swallow a keystroke. The label also never left its container's box. What survives of hypothesis A is the half no assertion reaches: `updateWysiwygStyle` runs on *every* scene update, sets `editable.scrollTop = 0` and refocuses the textarea, so the cure for it is having fewer updates, which is what this is. The measured, confirmed defect is therefore the flicker's trigger, not the loss. **Two whole-scene writes issued after their own awaits are guarded**: `refreshProjectBoard` asked once, before a `gh` read and two more requests, and asks again immediately before `renderMirror`; `reconcileDrafts` had no editor guard at all and now returns rather than writing, since its blocks are already gone from the server and the next poll reaches the same line with the caret gone. Those two close a race that cannot be forced from a browser, so they ship on inspection and the log says so. Log only: no file, route, block kind or feature moved — a check script adds none of them. |
| 2026-07-28 | #130 | #143 | A mirror card offers its issue URL **once**, through `customData.issueUrl`. It carried the same URL twice, and the second copy — `element.link` — was read by nothing in `src/` or `frontend/`; what it did buy was Excalidraw's own hyperlink UI, which keys off that property alone. The popup is drawn *above the selected element's own box*, so in a column it lands on the cards stacked over it, which is what the screenshot on the issue shows. Two copies of a URL, one covered card, and the panel already offered it as a real anchor. **Dropped on the cards, kept on the title strip**, which the issue left open: the strip is full width, so its popup sits over the mirror rather than over a card, and `board.projectUrl` is read nowhere else in `src/` or `frontend/` — dropping it would remove the board's only route to the GitHub project. **`link` as a general element property is untouched.** Hiding `.excalidraw-hyperlinkContainer` in CSS, or forcing `showHyperlinkPopup: false`, would take link editing away from every shape on the canvas to fix two generated ones; the browser check's fourth case draws an ordinary linked shape and asserts its popup *does* appear, which is both that boundary and the control — a check that only ever asserts an absence passes just as well on a harness that cannot see a popup at all. No migration: mirror cards are kept out of the autosync and redrawn from GitHub on every read. Both checks failed against the old code, the browser one stating the defect as geometry — the popup's box overlapping the box of the card above it. |
| 2026-07-28 | #124 | #142 | `⧉` detaches a terminal tab to the **left** of the block it came out of, not to the right. The direction is not a taste: the region is anchored to the far left, one `TERMINAL_GAP` from the GitHub mirror, and #96 moved it there because the documentation grows down and right so the left is the edge nothing runs into. The detach was written as "next to the block it came out of" with no reference to that, and authored new blocks straight back into the direction #96 had emptied — from the anchored origin the very first one landed 80 into the gap the region keeps and 680 units on top of the mirror. **Unconditional, not adaptive**: the observation said "always", and a rule that picked the emptier side would put the block somewhere the reader cannot predict, which is the same complaint. The 40 between two terminal blocks is unchanged — it is an intra-region separation and distinct from the 120 the region keeps from the mirror. The reason it survived is that nothing asserted a side: `check-terminal-tabs-browser.mjs` asked only that the two blocks were not on top of one another, `\|Δx\| > 40 \|\| \|Δy\| > 40`, which passes for either direction and would have passed for a block sitting squarely on the mirror. It now asserts the side, the inherited width, height and `y`, and that the new block clears the region the source was anchored beside — the last one through the board's **own authored content**, which with no `githubProject` configured is exactly what the source was placed one gap to the left of, so the mirror case is checked without a `gh`. Two of the three failed against the old code. Left out and worth its own issue: two detaches from one block with nothing moved in between still land on the same coordinate, since `beside` is read from the source both times — symmetric with the old behaviour rather than caused by this. |
| 2026-07-28 | #127 | #141 | A queue on the **Todo** header: on, and the server starts the oldest Todo issue every time an implementation slot frees. The cap already refused — the fifth click is told to come back later, and coming back later is a person watching the board — so what this adds is the deferral, not a second way past the limit. **Every start goes through the same `POST /api/implement` a click does**, which is why `beginImplement` now *returns* its answer rather than writing a response: the queue is a caller with no request to answer and needs the status, since `409` is precisely how it is told the slot went to somebody else. The claim before the first `await` stays the one arbiter of the cap. **On the server, not in the browser**, because the mirror's poll is gated on tab visibility on purpose and a queue that stopped when the tab was hidden would stop during the hours it exists for; a run settling dispatches directly and a timer (`EXCALIDRAW_IMPLEMENT_QUEUE_MS`, 30s) covers only what this process cannot see, and does not exist at all until a queue is on. The column is read **uncapped** — reading the drawn mirror would drain what fits on screen and silently never reach the rest, which is the case the check makes load-bearing with a board whose every drawn Todo card is closed. Five open questions answered: the drawn order flips **for Todo only**, because drawn newest-first the card at the top is the one starting last, while flipping every column would move a long `Done`'s truncation onto its newest cards; a failed run is **skipped and never retried**, since any record makes a card ineligible, which stops a backlog burning against a broken build without inventing a third "halted" appearance; the button is unlocked on a locked header, as `pb-add` already is; the switch is **not persisted**, because a server that came back up and resumed starting agents would be acting on a decision made before whatever brought it down; and the two appearances differ in **fill and outline weight**, not hue, which is already spoken for by the column and by a failed move. The toggle is *selected* rather than clicked and is deselected immediately — a selection that has not changed is ignored, so a button left selected could never be switched back, which is the case the browser check exists for. `check-implement-queue.mjs` failed 22 cases against the old code and `check-implement-queue-browser.mjs` failed at "the toggle to be drawn"; the second one also watches the drain itself — a card dashed, its run released, and the next issue starting with nobody clicking. Found and not folded in: the structure map still says `src/server.ts · 27 routes`, and there are 48. |
| 2026-07-28 | #122 | #139 | The board stops passing its inherited `CLAUDE_CODE_CHILD_SESSION` on to the children it spawns. Claude Code sets that marker in every subprocess it starts from a Bash, PowerShell or Monitor tool call, from a hook and from a status line command, and an interactive `claude` that sees it is excluded from `--resume`, `--continue`, up-arrow history and `claude agents`. It is deliberately *not* set for stdio MCP subprocesses, on the documented grounds that they are long-lived and outlive the session that spawned them — **this server is that class of process and was getting no such exemption**, so a board started once from a tool call stamped a marker that was stale within minutes onto every shell and every agent it opened for the rest of its life. **Stripped rather than overridden.** `CLAUDE_CODE_FORCE_SESSION_PERSISTENCE=1` is what the original observation literally asked for, and it is worse in one specific way: it overrides the exclusion unconditionally, including for a session that really is nested. The two compose if stripping is ever not enough, and `terminal.md` says so rather than leaving the next reader to rediscover it. **One function for both spawn sites.** `agentEnv()` replaces the two copies of `{ ...process.env, PATH: agentPath() }`, because a rule kept for the shell and not for the agents is how the two drift apart — the agents run `claude -p`, which persists either way, so they are covered for consistency rather than for need. The key is removed by case-insensitive comparison rather than by `delete env.X`: a Windows environment block is case-insensitive but a spread `process.env` is an ordinary object, so a differently-cased spelling would have survived a delete written in capitals and reached the child anyway. Nothing else is filtered — no allowlist, no per-project environment facility — and the check asserts a sentinel variable arriving intact so that stays true. **The issue’s own open question, whether a session had already been lost this way, is answered yes, by running it both ways.** Through the block on a board holding the marker, `claude` started, answered a question, and `claude --resume <id>` afterwards reopened that session with its own answer still in it; the same shell spawned with the marker still set wrote **no session file at all**, under Claude Code’s own status-line warning that transcript saving was off. These were not sessions hidden from a picker, they were sessions never written — which is why the by-hand half of this was worth its price. `check-child-session-env.mjs` is new and was written first, red on exactly three cases against the old code — the PTY path, the pipe path and the agent path — with its sentinel and `PATH` assertions green, so the harness was known good before the fix went in. One merge note worth keeping: #136 landed mid-flight and had independently bumped the same “All N were written first” sentence in `terminal.md`, so git merged two count bumps into one and left a thirteen-item list claiming twelve. A clean merge that was quietly wrong. Log only: no file, route or block kind moved. |
| 2026-07-28 | #123 | #138 | `check-issue-cache-browser.mjs` counts reads per selection, with the server's issue memo turned off rather than turned down. The two red cases were the *expectation*, not the cache: the extra invocation is a plain revalidation, one per selection, which is exactly what `DocsPanel` and `issue-cache.ts` say a selection is — what the session cache buys is the frame, not the freshness. The stub log settles it, six selections and six `gh issue view` calls, each 50–500 ms after its own click and none for a card nobody clicked. The old counts, 2 and 1 over three and two selections, were only true if the *server* memo swallowed the second selection of each pair, and this file turned that memo down to four seconds while a click-away between two selections costs about five. **So section 7 was an assertion about how fast this script runs**, and the reason it stopped holding is not in the panel at all: three of the six points `deselect` can reach sit under Excalidraw's own left-hand properties island, which is on screen precisely because something is selected, so each of those clicks is swallowed and burns the helper's full 1200 ms before a usable point is found. No commit changed when a read is issued; the panel has read once per selection since #74, the commit that wrote this check. **Off, not down**, which is what makes the file's own stated division of labour true — `check-issue-cache.mjs` counts the memo against a clock it controls, because no browser is in the way, and this one counts the panel. Section 7 then asserts the rate the panel owes: one read per selection, never two, and nothing read that no click asked for, counted against the selections the script itself made rather than totals written by hand, so a case added above cannot leave a stale total below. **The rewrite is strictly stronger than what it replaces**, which is the part worth reading: with the memo on, a panel that read twice for one click had its second read joined to the in-flight memo entry — `IssueMemo.read` registers before it awaits — and was counted the same as a correct one, as would be a mirror that read every card it drew. Neither was catchable here. Verified in that order: a duplicated `fetch` in the revalidation effect puts all four new cases red, and reverting it puts the whole script green. Section 5 loses its `sleep(MEMO_MS + 500)` with the memo gone, and section 2's heading now says what it asserts rather than what the counts claimed. Noted for an issue of its own: `deselect()` spends about 3.6 s per call clicking into the properties island, which costs wall-clock in every browser check that copies the helper. Log only: a check script adds no file, route, block kind or feature. |
| 2026-07-28 | #118 | #137 | The fields a research run writes are taken out of the sync's version race, and a block that lost them can be told its issue. **The run #118 asked for was captured, and hypothesis 1 is what it shows.** `check-issue-state-sync-browser.mjs` drives a real board through a whole run with both sides instrumented and prints what each held: six arrow keys mid-run put the browser at `version` 8 while the store held 3, and it stayed there for two seconds. `version` is a number the browser owns — Excalidraw bumps it on every keystroke, drag and nudge, against one bump per state change here — so `POST /api/elements/sync`, which merges whole elements with the higher version winning, hands the browser the whole element whenever it is ahead. A payload built before a server write and applied after it reverts that write entirely, and what it reverts to for a block whose run started while the reader was still working on it is the pristine draft: no `issueState` at all, which is exactly what the three blocks behind #94, #95 and #96 still carry. Hypothesis 2 is ruled out rather than left standing — #100 already made a refused `scheduleAutoSync` something `releaseAutoSyncSuppression` comes back to, and the timeline shows the syncs arriving. **Candidate 1 was taken**: `element-authorship.ts` names the ten fields this server authors and the merge restores them onto any payload that disagrees, with the appearance the state is drawn from, since a created block wearing the dashed outline is the third of the three symptoms. Both directions, which is the same defect read backwards: a field the store does *not* hold is removed from the payload, or a browser still holding `running` after a reset would reinstate it on the next autosync. Everything else stays the browser's, `version` included, and a payload that already agrees is returned untouched — asserted as its own cases, because a merge that answered this by ignoring the browser would break what the sync is for. **The crossing is milliseconds wide and stays racy in a browser**, so the reproduction that discriminates is the deterministic one: `check-issue-state-sync.mjs` sends the store the payload the browser check proved this board really produces, and goes 15 cases red against the old code with `url=undefined` among them. The browser check's own measurement passes either way and says so in its header — being ahead is the precondition, not the defect — and what it buys is that the payload is a thing that happens rather than one invented in order to fail. **The way back is an affordance rather than the issue's candidate 3.** A weaker `reconcileDrafts` match on a quoted observation rests on an agent's habit rather than a contract, and the issue says it does not close this on its own; so `POST /api/issue-block/:id/adopt` tells a block which issue it already produced, making the same two writes the end of a run makes, and the panel offers it as **This block already has an issue** — below the run and worded as a fact, because the wrong reading of it is the button directly above. It creates nothing: the URL names an issue that exists, `gh` reads it, and a URL it cannot answer for is a 502 with nothing written. Loopback only, guarded like the read route rather than the run route. Structure map: the sync card names the exception, and the issue block card says the state is the server's and a lost one can be adopted back. |
| 2026-07-28 | #112 | #136 | The terminal's screen takes the pointer and its header keeps the block. The overlay was transparent everywhere except a strip along the bottom saying `click here to type`, on a reasoning written down three times: an overlay that swallowed clicks would take Excalidraw's resize handles with it. The issue measured it and it is wrong — the handles sit 2 to 10 screen pixels **outside** an element's bounds and the overlay covers exactly the bounds, so the screen can take every click it likes and the block keeps every handle. What it really costs is selecting and dragging the block from its middle, which is what the observation offered to trade, and the **header is what buys it back** — load bearing for resizing too, since a handle only exists while something is selected. The maintainer's comment is what settles the four open questions, and it asks for more than the body did: select, scroll, copy and paste. So the **wheel scrolls the scrollback**, and a wheel the scrollback cannot use is **re-dispatched at the canvas** rather than dropped, because the issue is explicit that swallowing it fails either way — at the canvas rather than at the container that listens, whose handler ignores any wheel whose target is not a canvas, a textarea or an iframe, and re-dispatched at all because this overlay is a sibling of the canvas and is nowhere on the event's path. **Ctrl+C copies while something is selected and interrupts the rest of the time**, Windows Terminal's bargain, dropping the selection so the next press is an interrupt again — without that a reader who had selected something would have no way to stop a shell running away. **Ctrl+V is left to the browser** rather than handed to the shell as the `\x16` it means on a terminal, which xterm's own key handling would otherwise have sent *and* cancelled, killing the paste event xterm already listens for. Alt+click moving the shell's cursor is xterm's own and nothing was added for it. Two calls the repository did not settle: the strip survives as a **status line**, because a band that reads like a prompt and does nothing is worse than either; and the header gets a **minimum height of 12 screen pixels**, because everything on this overlay is sized in `em` and the only draggable band was about six pixels tall at the zoom floor — a no-op above about 70% zoom, and it changes nothing any shell is told, since the grid comes from the block's scene size and not from the frame on screen. Mouse reporting is left as xterm's default and written down instead: a program that turns it on takes the pointer with it and cannot be selected inside. `check-terminal-focus-browser.mjs` was seen to fail nine cases first, and the copy case is the one worth reading — against the old code it went green on the *wrong* thing, because the keystrokes reached the canvas and Excalidraw put a diamond on the clipboard; what caught it is asserting the copied text was text on the screen. Its clipboard permission is granted over Chrome's **browser** target, since `Browser.grantPermissions` is refused on a page session and `NotAllowedError` is indistinguishable from the block never asking. Found and not folded in: `check-terminal-tabs-browser.mjs` case 8 already fails on `main`, `fitBlocks()` leaving the merged block's corner off the window. |
| 2026-07-28 | #121 | #134 | The overlays that sit outside the canvas are given the canvas's own theme tokens, declared on `.app[data-theme=…]` in `frontend/index.html`. The documentation card is styled with Excalidraw's variables so that it follows the canvas into dark mode, and it is deliberately a *sibling* of `<Excalidraw>` rather than a child of it so that it can never become a scene element and reach a PNG export — but Excalidraw scopes those variables to its own root element, so outside that subtree they were undefined in **both** themes. Every declaration was painted from a hard-coded light fallback, and the ones with no fallback were invalid at computed-value time, so `color` fell through to the page's inherited `#e3e3e8`: near-white text on the white fallback card, **1.28:1**, measured in the browser before anything was changed. Light mode had the same defect and nobody noticed, because there the fallbacks happened to match. **Copying Excalidraw's values rather than inventing a palette**, which the issue left open: the card is meant to look like part of the board, and the cost — an upstream rename has to be followed here — is what the new check notices. **The grey ramp is the one place a straight copy does not work.** Excalidraw does not invert it per theme; it keeps one ramp and picks a different step per rule under `.theme--dark`, which a stylesheet outside its subtree has no way to do. The ramp is mirrored instead, n to 100-n, so each token keeps the role it already had: 10 the quiet fill behind code, 30 a border, 60 and 70 muted text. `DocsPanel.css` then drops its fallbacks outright — they were light values, they were what the card was actually painted with in both themes, and pairing one with a dark inherited foreground is precisely what turned stubbornly light into unreadable. **The part worth reading is what nearly shipped.** With the foreground themed and `--color-primary-light` left out of the list, the selected project tab kept its light fallback and took the new near-white label — the fix introducing the defect it was fixing, one tab to the left. Declaring *part* of the set is worse than declaring none, so the list is every variable the overlays reference, and `check-docs-panel-theme-browser.mjs` covers the tab strip and the settings dialog as well as the card. It reads the **rendered** colours with `getComputedStyle` in real Chrome or Edge, in both themes, because variables resolve at computed-value time and neither `tsc` nor `vite build` ever runs that — the third defect in this UI layer to compile perfectly and do nothing it claimed. Written first and run against the old code: 10 cases red, the 1.28:1 among them. Noted and not fixed, for an issue of its own: `.workspace-dialog__actions button` (0-1-1) outranks `.workspace-dialog__submit` and `.workspace-config__save` (0-1-0), so the primary styling on those two buttons has never applied. Log only: a colour fix in an existing panel adds or removes no file, route, block kind or feature. |
| 2026-07-28 | #115 | #133 | The terminal is redrawn in the board's design: paper, and the monospaced sibling of the face the blocks are lettered in. It was the one region of this board in another design language — a Catppuccin Mocha surface on a `#1e1e2e` shape, in whatever monospace stack the machine resolved — and this reverses a decision written down twice: *a terminal that follows the canvas into light mode stops looking like one*, and *a dark fill is what reads as a terminal at a zoom where the shape is all there is to read*. Both were about a board that has since become a hand-drawn pastel document. **One palette, in `terminal-palette.ts`.** Three things draw this block — an Excalidraw rectangle, an xterm theme object, a stylesheet — and each carried its own hexes, already drifted. A stylesheet cannot import TypeScript, so the overlay writes `TERMINAL_CSS_VARS` onto its own root, on the *card* rather than `:root` so two blocks are two surfaces, and the CSS holds no colour. **Excalifont cannot drive an emulator** — xterm puts column N at N × the cell and it is proportional — so the observation is honoured through Comic Shanns, which Excalidraw ships as its code face and registers on `document.fonts` whether or not the scene uses it, so nothing is shipped or preloaded. The issue flagged "metrically monospaced" as its one unmeasured assumption; measured first on a real render, it is exact: 0.55 per font pixel at every size in the range. **All twenty-one theme entries are set.** The old five left sixteen at xterm's dark-tuned defaults, which on paper is yellow at 2.3:1 and bright white at 1.1:1 — not a colour anyone chose, the colour nobody set. These are Latte hues darkened until each clears 3:1, and the grey ramp is read as *contrast* rather than lightness, since on paper "white" is the page: `brightWhite` is the strongest ink and `brightBlack` the dim one tools use for comments. The cost — `black` and `white` as two similar greys — is inherent to printing sixteen colours on paper and is written into `terminal.md` rather than hidden. **The dark fill's other job moves to a band**: the header and prompt rows become solid dark strips, so the block reads as a paper card with a bar top and bottom, and an area survives being shrunk where a glyph does not. **A typeface change moves the grid metric, and that is the other half of the work.** The width had been a constant because there had been one typeface; Comic Shanns advances 0.55 against the stack's 0.586, which is seven columns of a default block, so the advance is measured in the browser exactly as #125 made the line box measured — and xterm does no arithmetic on it at all, `device.cell.width` *is* the measured advance. The fallback stays at the widest face the stack can resolve: too narrow clips, too wide costs a column. `TERMINAL_LINE_HEIGHT` drops to `1`, because the new face's own line box is 1.72× the font against Cascadia's 1.15 and the old 1.35 on top of it would have spent seven of the default block's twenty rows on leading nobody asked for. **Two consequences of the face being loaded rather than present, neither visible in the source.** xterm measures its cell at `open()`, so a face landing afterwards is drawn but never measured; opening later would mean no emulator while the transcript arrives, so the measurement is redone instead — by naming the fallback and then the real stack again, since xterm's options service fires only on a value that *changed*. And a block placed before the face arrives reported the fallback's grid with nothing to correct it: the error is always in the safe direction, a narrower terminal rather than a clipped one, so no assertion anywhere would have gone red — `App.tsx` re-reports once the face is in. The open questions are answered rather than left: paper in both themes, Latte rather than the pastels (five pastels cannot yield sixteen distinguishable slots), one look rather than a reader preference, the bands for the zoom, and `COLORFGBG` deliberately untouched. `check-terminal-paper-browser.mjs` is new, written first, 10 cases red. `check-terminal-rows-browser.mjs` gains the width half rather than repeating it, per the definition of done, and was verified the other way round — the face swapped with the cell left behind puts it red at all three sizes, 97 columns of 104 at 13px, 159 of 170 at 8px, 51 of 55 at 24px. Log only: the palette is a module, and the map names entry points, blocks and routes rather than the files behind them — the same call #125 made for `terminal-metrics.ts`. |
| 2026-07-28 | #116 | #131 | A board is given a docs directory when it plainly has one, and told the truth when it has none. Documentation reaches a board through `docsDir` and nothing else, and a project added through the `+` was written `{ name }` — deliberately, so a new tab does not arrive marked broken — which made every `docKey` on it answer 404. The panel then mapped every 404 to `missing` and threw the body away, so a board one setting away from working reported `No document yet for <key>`: a per-document problem, for a project-level cause, with the field that fixes it sitting unmentioned in a dialog the reader had no reason to open. **The seeding is a creation, not a repair.** `ensureWorkspaceConfig` stats `<project>/docs` and writes `docsDir` only if it is really a directory — read from disk, never guessed, because a path written into somebody else's config that nothing resolves is worse than the blank the settings dialog offers. It runs when the config is created and at no other time: an already-registered project keeps its config exactly as it is, absence included, and the check holds that to the file's mtime rather than to its contents. `granjai` is repaired by hand, in the dialog, which is also the only honest answer for it — its markdown is spread over four folders and a flat `docKey` reaches none of them, so `docsDir` corrects its *diagnosis* and its mirror block without making its documents addressable. Nested keys stay the issue's open question. **The route had the answer all along and no way to say it.** Both failures are 404s and both are correct as 404s, so they gained a `code` — `no-docs-dir` and `no-doc` — rather than a status change; the prose stays for anything reading the API by hand, and the panel switches on the code into a state of its own that names **Docs folder** and the gear it lives behind. **A tool block's documentation is a property of the tool.** The mirror is generated onto every project naming a `githubProject`, always carrying `docKey: "project-board"`, which resolved inside the *mirrored* project — so on every board but this one the mirror read as undocumented, on a project whose `docsDir` was set correctly. `TOOL_DOC_KEYS` resolves those against the tool's own `docs/`, and tool keys win over a project file of the same name: a board silently explaining the mirror wrongly is the worse failure. `docs/*.md` joins `package.json`'s `files` for the same reason one layer out — the server now serves its own documents, and an installed package shipping none would answer 404 for exactly the block this fixes. Both checks were written first and are why the copy could be trusted: the route check failed 10 cases, and the browser check failed 3 on the sentence the issue quotes. The browser one reads `.docs-card .element-docs` rather than the card, because the card's header is a close button that renders before the fetch lands — a check reading the whole card answers `×` for a panel that has decided nothing, and passes by racing the fetch rather than by the panel being right. |
| 2026-07-28 | #114 | #129 | Worktree branches are cut `--no-track`, so none is handed to an agent tracking `main`. `git worktree add -b issue-N <path> origin/main` takes the remote-tracking start point as an upstream to configure and writes `branch.issue-N.merge = refs/heads/main` — not a deduction, it is sitting in this repository's own config against `issue-107` and `issue-112` today. The harm is `git pull`: run in the worktree before the agent's first push, it fast-forwards the feature branch onto whatever the default branch gained while the agent was working, silently, because that is precisely what a correct upstream would mean — and with four implementations running and self-merging at once, "the default branch gained something" is the normal case rather than the unlucky one. **None rather than a different one**, because the upstream an agent actually wants is `origin/issue-N`, a ref that does not exist until the agent's own `git push -u` creates it and rewrites both keys correctly; writing a placeholder would mean taking `.git/config.lock` to write something wrong. The issue's open question is answered rather than assumed — nothing reads that config before the first push, not the server, not the prompt, not a check, and `worktreesHoldingWork`, which arrived from #126 while this was being written, counts against `baseRef()` explicitly rather than against the branch's upstream. The one behaviour that does shift is `releaseWorktree`'s `git branch -d`, judged against `HEAD` alone once there is no upstream to judge against; under a squash-merge workflow a branch is never `merged` by git's reckoning either way, so it refused before and refuses now. The flag goes on the `-b` form only: checking out a branch that already exists configures nothing, measured rather than assumed, as was the flag itself. **`check-worktree-config-lock.mjs` had to change, and that is the part worth reading.** Its first two sections asserted the thing this removes — that a held `.git/config.lock` is *waited out*, and that a lock nobody releases is fatal. With no branch config to write, `worktree add` no longer takes that lock at all, so they now assert a held lock is survived outright, with the lock still held when the worktree comes back and no branch config written to contend over. The wait from #113 stays and stays enforced, because the writers it covers were never ours: an agent's own `git push -u` takes that same lock from a process the server cannot serialise and this check cannot provoke, so the retry and its names-the-lock-file failure are pinned on the source — the way section 4 already pins what the serialisation queue encloses, and for the same reason. Rewriting a check beside the code it covers is exactly the failure `CLAUDE.md` describes, so it was verified the other way round: `--no-track` stripped out of the built output puts three cases in section 1 red. `check-worktree-upstream.mjs` is the new check and covers both halves the definition of done names — the state a worktree is handed over in, and what the first push does with it — failing four cases against the old code, the `git pull` fast-forward among them. Log only: a flag on a git invocation adds no file, route, block kind or feature. |
| 2026-07-28 | #107 | #126 | A run killed with its server comes back as `interrupted`, and can be resumed. It used to come back as *nothing*: the state is a `Map`, so a restart erased the fact that five runs had ever happened, while the cards sat in **In Progress** exactly where a healthy run puts them and one worktree held 377 lines that existed only as a dirty working tree. **Detection is derived rather than kept** — option B, and the reasoning is that a file can be written and never cleaned, or cleaned and never written, whereas the worktree *is* the work and so cannot disagree with itself; it is also true for a run the server never got round to recording, which no persisted record can be. `implement-recovery.ts` walks `git worktree list` at startup, keeps the `issue-<n>` checkouts under the project's own worktree root, and reports any with commits the base does not have or a dirty tree. A clean one is reported as nothing, which is an answer rather than a gap. The issue URL is reconstructed from the checkout name plus `repo`, then `origin`; a board declaring neither gets a warning rather than a URL pointing at somebody else's issue. **D was investigated and does not compose with B.** `claude --resume <id>` is real and does work alongside `-p`, but detection here persists nothing and a session id is not in the worktree — carrying one across a restart is a file on disk, which is option A, the persistence the scope declines. It would also mean appending flags to a command line somebody else wrote and assuming it is Claude Code, the same rewrite `agent-usage.ts` refuses for `--output-format`. So C: `resume: true` on the existing route, refused with 409 unless the server agrees there is an interrupted run, and one paragraph added to the prompt saying what the previous attempt left, that none of it was reviewed or explained, and to read it before deciding whether to build on it. A flag rather than a route of its own because everything around it — the per-issue guard, the cap, the worktree, the release — is identical, and a second copy would be a second place to get the one-issue-one-pull-request guard wrong. **Resume sits beside Implement / Fix rather than replacing it**: continuing somebody's half-finished change and throwing it away are both defensible and have to stay two decisions. Nothing commits on anyone's behalf, and no card is moved — the argument that the server writes the moves, that the agent cannot report its own crash, says nothing about who undoes one when the *server* crashes, and a card walking backwards while a person watches is worse than a stranded one. `interrupted` draws nothing on a mirrored card, like `failed`, for the same reason. The price of deriving is written down rather than hidden: a run that finished and left a dirty checkout is indistinguishable from one that was killed, so it comes back as interrupted too, and `DELETE` clears the record only until the next startup finds the worktree again — the panel says the way out is to finish or discard what is in the checkout. Two checks, both run against the old code first: the server one failed 7 cases (`[]` from the restarted server, resume accepted for an issue with none, no paragraph, no `offersResume`), and its temp directory is deliberately *not* named after the feature, because the worktree path ends up inside the prompt and a directory called `…resume…` satisfied the assertion about what the agent was told with none of the feature written. The browser one is the half only a browser answers, and is why the panel copy could be trusted. |
| 2026-07-28 | #104 | #125 | A terminal row is the font's own line box, measured, rather than `1.35 ×` the font. That 1.35 is the `lineHeight` xterm is *given*, and xterm never applies it to the font size: it measures the font's line box first — `fontBoundingBoxAscent + fontBoundingBoxDescent` — and multiplies that, `floor(charHeight × lineHeight)` in `DomRenderer._updateDimensions`. For this stack the box is a little over `1em`, so a real row was nearer `1.55 ×`, the block divided by a cell 15% too short, and the shell was told rows the frame had no room to draw — 22 of 20 at the default size with no button pressed, and with a PTY behind the session those are rows a full-screen program is repainting into. **The open question was whether the ratio is a constant at all, and the answer is that it cannot be.** The metrics come back as whole pixels, so the ratio is a staircase and not a line: 1.50 at 8px, 1.40 at 10px, 1.60 at 20px, 1.54 at 24px on the machine it was measured on, and every member of `TERMINAL_FONT_FAMILY` has a box of its own, so a second measured constant would have been wrong at most sizes even where it was measured. So the line box is **read** — `frontend/src/terminal-metrics.ts`, by the same `measureText` route xterm's own `TextMetricsMeasureStrategy` takes, so what is read is the number the emulator will divide by rather than an estimate of it — and `terminalCell()` does xterm's arithmetic on it, `ceil` and `floor` included. `terminal-block.ts` stays DOM-free so the offline checks still import it, and a caller with nothing to pass falls back to `TERMINAL_LINE_BOX`, rounded **up** from what this machine reports: too tall costs a row at the bottom, too short costs rows nobody can reach. Not cached by size, because the answer can change under the page and a wrong first answer would have nothing to invalidate it. The font family and the line height move into that module and the emulator is opened with both, since a grid derived from one font and drawn in another is two fonts agreeing on a number. The width is untouched — linear at 0.586px per font pixel against the 0.585 the block is drawn with — which is what #95 had already established and deliberately left. The cost is the behaviour change #95 held it back for: every board's grid shrinks, 22 → 19 rows at 13px. `TERMINAL_CHROME` is a shade generous, so the new numbers are conservative by up to two rows at the largest font; that is the safe direction and it is bounded rather than left open. The check is browser-only on purpose and it is the reason the fix could be trusted: an arithmetic check that divided by the same wrong cell would have agreed with the code. It runs at zoom 1, where a scene unit is a pixel and the question is well posed, and sweeps the default and both ends of the range — 7 cases red against the old code. Log only: no route, block kind or feature added or removed, and the map names entry points rather than the modules two of them were added under. |
| 2026-07-28 | #110 | #120 | The project tab strip gets one scale, set to 2.5x. Nine hard-coded pixel constants are why the board's top-level navigation ended up the smallest text on the screen — a 13px label under a 24px `<h1>`, beside a documentation panel at 0.875–1.125rem — and why there was no lever to move: `WorkspaceTabs.css` had no custom property, no `rem` and no `em`, and the board's zoom does not reach DOM chrome. `--workspace-tab-scale` on `.workspace-tabs` is that lever, with every length in the file `calc(<base> * var(--workspace-tab-scale))` and the bases left literal so the strip's proportions stay readable. **2.5x is taken literally**, as the issue said it would be unless answered otherwise: the label is now larger than the app's own `<h1>` and the strip costs the canvas about 50px more, and the token is exactly what makes revising that one number rather than a hunt through a dozen declarations. Three smaller calls. The property sits on `.workspace-tabs` rather than `:root`, because it describes this strip and a board-wide token would invite `WorkspaceDialogs.css`, which runs the same 13/15/12 scale, to inherit a size nobody asked it to take. The gaps and the radii scale too, past the six declarations the issue lists, because a 2.5x name in a 6px gap sits on its badge and a 6px corner on a 73px tab reads as a square. The `1px` borders do not: a border separates rather than being read, and a 2.5px outline round the active tab draws more attention to the frame than to the name in it. The check measures the browser rather than the file, because `font-size: calc(13px * var(--workspace-tab-scale))` is valid CSS that renders at 13px if the property never reaches the element — precisely the class `CLAUDE.md` says compiles perfectly — and it fails 13 cases against the old stylesheet, naming every constant it found. Two of its questions are the size's consequences rather than the size. The strip is asked to overflow at a 620px viewport, since wider tabs reach horizontal scrolling with fewer projects and scrolling is what keeps a fourth project reachable; and `appState.offsetTop`, the offset every anchored overlay is drawn through, is asserted to equal the canvas's real top edge. That was the issue's largest worry — the strip renders only once `GET /api/workspaces` resolves, so it grows after Excalidraw has mounted, and a stale offset would land the documentation card and the terminal off by ~85px instead of ~34px. It does not go stale, and the reason is worth writing down: `.canvas-container` is `flex: 1`, so a taller strip changes the container's *height*, which is a resize Excalidraw already watches for. Confirmed in a browser on both overlays. Log only — a size adds no file, route, block kind or feature. |
| 2026-07-28 | #117 | #119 | Every draft is drawn in the notes column, and the stamp it carries decides nothing. Rehoming keyed on ``the board no longer has this column`` for one release and caught half the population: a block carries whichever column the `+` was on when it was clicked, so while that column was an ordinary option any change to the project *ordering* stranded every block already written — with a stamp naming an option that is still perfectly real. Three of those sat among the issues in `Todo` on project 5, unmovable, because `settleMirrorDrag` rewrites a column for mirrored cards and nothing else. The rule that catches both is the simpler one: a draft is an observation, and observations are in the notes column. Overruled at layout time rather than rewritten onto the element, because a correction that has to be written is one that can fail, be refused, or lose a race with the autosync. |
| 2026-07-27 | #101 | #113 | A worktree waits for `.git/config.lock` instead of dying on it. Every worktree shares the main repository's `.git/config` — there is a per-worktree one only under `extensions.worktreeConfig`, which is not set — and the branch is cut from `origin/HEAD`, so `worktree add` sets up upstream tracking, writing two keys in two transactions and taking the lock for each. Git neither waits on that lock nor retries, so two implementations starting in the same second left one dead before its agent was ever spawned. Waiting is what removes the class rather than serialising, because the other writers are not ours: every agent in a worktree eventually runs `git push -u`, writing those same two keys, and a process the server does not own cannot be queued. The server's own call is serialised as well, around the git call only — `linkDependencies` is the expensive half and writes no config — so contention we cause ourselves never reaches the retry. Reproducing it deliberately found the thing the issue does not say: **a failed attempt is not a no-op**. Git rolls the checkout back but keeps the branch it just created, so the identical command a second time answers `a branch named 'issue-96' already exists` — a retry that did not undo the attempt would have turned a lock collision into a permanent one. The branch is deleted between attempts, which also brings back the upstream the failure lost rather than quietly settling for a worktree without one. A lock still held after the window is reported *by name*: a stale `.git/config.lock` from a crashed git is a real state, and the difference between naming it and a generic worktree error is a one-line fix versus an afternoon. The window is 2s rather than the ~1s assumed — the assumption that a real lock is held for milliseconds is right, but on Windows each attempt costs a process spawn, so a second buys one or two of them. `--no-track` was measured as the issue asked — it does succeed with the lock held, writing no branch config at all — and split into #114, because the upstream currently written is `issue-N → origin/main`, which no feature branch wants, and that is a behaviour change of its own. Two notes from writing the check, both of which cost a draft: the fixture needs a bare `origin` and `origin/HEAD`, since a base that is a local branch takes no lock and proves nothing; and the lock has to be held for longer than the few hundred milliseconds `ensureWorktree` spends on `worktree list`, `show-ref` and `symbolic-ref` before it reaches the call that takes it, or the old code passes for never having collided at all. The last case is asserted on the source on purpose — `linkDependencies` is synchronous, twenty thousand `linkSync` calls that block the event loop, so two worktrees can never be *seen* linking at once however the lock is scoped; what is checkable is what the lock encloses. |
| 2026-07-27 | #97 | #109 | The column observations are written in gets an id of its own, so no `Status` option has to stand in for it. It was drawn on `index === 0` — whichever option the project declared first — and the `+` stamped that option's id onto every block it dropped, which meant GitHub was being asked to keep an empty option alive to lend its id to a population GitHub never sees: project 5 declared `My Notes` and held 48 items, none of them in it. The cost was invisible until the option went, and then it was two things at once — the `+` lands on `Todo` and observations share a column with real issues again, undoing #86/#91, while every block carrying the old id names a column the board no longer has, placed nowhere and counted by no header. So `NOTES_OPTION_ID` is reserved the way `NO_STATUS_OPTION_ID` is, and shaped so it cannot be mistaken for an option: the `:` in `canvas:notes` is refused by the `NODE_ID` pattern every write to the project is validated against, so a card dropped into that column cannot be written back even by a caller that tried — which is why the drop snaps back in silence rather than earning an error message for something this side already knows is impossible. `layoutMirror` is the new entry point and the only place that says the column exists; `layoutBoard` still draws whatever sections it is handed and asks nothing about where they came from. Rehoming is the one rule reversed rather than added: a draft naming a column that is not drawn used to be left where it sat, because none of the project's columns is where it *belongs*, only where it might be guessed to — the notes column is where it belongs by construction, and the blocks a deleted option leaves behind are exactly that population. Only unknown ids are rehomed, so the three drafts in `docs/board.excalidraw` carrying `f75ad846` stay in `Todo`, where they were already being drawn. The header is a draft count by the same sum as every other column with two of its three terms zero, not by a special case. The `Todo` move stays, but the comments justifying it by "the first column" go: adding #97 to project 5 by hand landed it in `Todo`, not in the first option, so *Item added to project* is configured here rather than assumed — and its configuration is not readable through the API, which is what makes the move worth having. Deleting the option from project 5 is still a maintainer's step by hand, for the reason it always was: `updateProjectV2Field` rewrites the whole option list from an input carrying no option ids, and every item's `Status` is stored against one. |
| 2026-07-27 | #102 | #111 | The implementation cap counts and claims in one step. It read the count and refused, then wrote the record that *is* the claim thirty lines later, with `await loadWorkspaces` in between — so two clicks arriving together both counted before either claimed, both passed, and the cap was exceeded by however many fitted in the window. The record write moves up to sit directly under the count; nothing between them yields, so there is no window left. What made this worth fixing beyond a budget overrun is what the extra run did: it put two `git worktree add` in the same instant, and they collided on the shared `.git/config` (#101). The **cost of claiming first was handled rather than ignored**, because it is the same defect pointing the other way — the two validations that now follow the claim, an unregistered workspace and one that failed to load, each release the slot before answering 400, or the cap would be held by runs that never started and shrink until the server restarts. The release **restores** the record that was there rather than clearing it, which the issue did not specify: a `failed` record with its error is what a block is showing when the reader clicks again, and a 400 that changed nothing must not wipe it. The check is the shape the defect demanded rather than the one that reads naturally — one `Promise.all`, because a loop with awaits lets every request finish claiming before the next one counts and cannot reproduce it at all; cap 1 and two requests, asserting exactly one 202. Its release cases pass against the old code on purpose: they guard the direction the fix introduces, and a check where every case goes red says nothing about which half of it is load-bearing. Log only — a race fixed adds no file, route, block kind or feature. |
| 2026-07-27 | #99 | #108 | The mirror is anchored **once** instead of measured on every poll. Its origin was recomputed each twenty seconds from `minX - MIRROR_GAP - boardWidth(sections)` and neither number was stored, which gave the region two independent ways to drift with nobody touching the canvas — and is why the observation could not say whether the trigger was time or an action. It was both. Its own width moved it because the region was pinned by its **right** edge and drawn leftward: a column added on GitHub did not extend it toward the board, it pushed every column already drawn 324 further from it, the first one included — which is where the `+` is and where the observation was written. `No Status` is appended only while something is in it, so that could happen and unhappen on its own, twenty seconds after an item lost its status and with no local action to connect it to. And the bounding box moved it because `minX`/`minY` were taken over *everything else on the canvas*, so any element added, dragged or erased anywhere that changed the scene's leftmost or topmost coordinate dragged the whole region along. `resolveMirrorOrigin` measures the first time there is something to measure against and the answer is kept; the empty-canvas fallback deliberately is **not** kept, or a poll that ran before the scene arrived would pin the region where an empty board put it for the rest of the session. **Which edge survives a width change was the open question, and it is the left one** — a mirror whose width is set by GitHub cannot keep both. The left edge is where the `+` sits and, since #106, the edge the terminal is anchored to, so a column now grows the region *rightward* into the gap it keeps from the board: a collision a reader can see and connect to the column that caused it, rather than a drift they cannot. The price is the terminal's own and it is written down rather than left implicit — a board whose content is moved wholesale leaves the region behind, and a reload re-measures. That trade is also what reconciles this with #106, which merged mid-flight and chose the terminal's side on the reasoning that "the mirror repaints every twenty seconds": this removes the repaint, and the two decisions turn out to need each other, because the old mirror grew *leftward* and would have stepped 324 onto the very block #96 had just placed behind it. Pinning the left edge is what makes `terminal \| mirror \| content` safe to stand on. Third, `mirrorAnchors` says in one place what the measurement may see, and adds the rule the autosync and the export already stated: a label bound to the terminal goes with the terminal. Excalidraw binds text to whatever is selected and that text carries no `kind` of its own, so a title bound to the one block the reader is expected to drag looked authored — drag it up and left and the mirror followed while the block itself was ignored. Both rules moved out of the component to sit beside the layout arithmetic, which is what lets a check ask them without a browser; `check-mirror-anchor.mjs` reads them out of the module *or reconstructs* them when they are not exported, so the old code fails on a region that walks rather than on a missing symbol — 12 cases, reproducing the issue's own numbers. Log only: no file, route, block kind or feature was added or removed, and no structure-map card states the placement rule. |
| 2026-07-27 | #96 | #106 | The terminal moves to the far left, past the mirror, and the canvas reads terminal \| mirror \| content. The arithmetic was never wrong; the pairing of side and cadence was. The block is placed **once** because the reader is expected to move and resize it, and it was sitting on the one edge this board grows into — the documentation grows down and right, so anything authored past `maxX` as it stood when the session opened ran into a shape that would not budge. The mirror never had that problem because it repaints every twenty seconds, which is exactly why it can afford to be the region in the way. So `terminalOrigin()` now takes the region to clear and answers `left - gap - width`, the mirror's own arithmetic applied a second time: the mirror when a project names one, and the mirror's own slot when none does. Place-once stays, and "the documentation grows down and right" is written down as the invariant rather than left as a preference — content that has to extend leftward is moved right instead. The one exception is the race the issue asked about and it is not hypothetical: a session opens on a `POST` that spawns a shell while the mirror arrives on a poll that spawns a `gh`, so the block is essentially always placed before there is a mirror to see, and the no-mirror fallback puts it *in the slot the mirror is about to take* — a collision, not a misplacement. It is marked `customData.awaitingMirror` when placed blind and the first board that lands moves it aside and takes the mark off, once, and only while it is still exactly where it was put. The map's own cards are updated as well as the log: `check-board-map.mjs` passes on the log alone, but two cards describe this layout in words, which is the case `CLAUDE.md` carves out of "a label repositioned belongs in the log". Found on the way: three browser checks were dragging the block's corner handle from the viewport `Alt+T` fits, where the vertex converted back through `(clientX / zoom) - scrollX` can land a hair outside the shape and the block moves instead of growing — invisible until the block sat at a negative `x` and the rounding went the other way. Each pins a viewport of its own now. |
| 2026-07-27 | #94 | #105 | The terminal block gets a strip of tabs, and a tab can be given a block of its own. One session per board was a **guard**, not an oversight — this runs whatever arrives over an unauthenticated API — so it is relaxed from 1 to N rather than removed: eight per board, and the ninth request is a 409 that names the cap. The five open questions were answered rather than deferred. Split is a second block on the canvas, because a detached tab is then an ordinary shape and moving it is Excalidraw's own; the gesture is a button — `⧉` detach, `⇥` merge into the nearest block — rather than dragging a chip between strips, since a strip taking drag events across the width of the block is more pointer than this overlay may take without costing the shape its handles. That is the same rule that makes the chips, and not the row they sit in, the part that takes a click. Scrollback stays 200,000 characters **per session**: a shared budget would let one noisy tab eat another's history, so how far back a board remembered would depend on which tab had been busy. A tab is labelled with the session id, and the block goes with its last tab, with `Alt+T` as the way back. Ids reach everywhere the old code said "the session": the routes, where the id is optional but its absence is never *guessed* at — one session is unambiguous, several is a 400 that lists them — and the socket, where a new `terminal_sessions` message replays every live session on connect as a whole set, so a viewer reconciles rather than only adds. One emulator per session, all kept alive, because rebuilding on a tab switch replays a transcript into a fresh parser and a full-screen program left in a background tab comes back as its own scrollback. Which tab is in which block is deliberately not saved — the blocks are derived — so a reload puts every live session back into one. Reconciled with #98 and #103, which landed mid-flight: the erase restore became the reconcile, per-session geometry and all, and the font size feeds the grid for every block. The tombstones now go *around* the conversion rather than through it, which is both what keeps an erase erased and what keeps a deletion reaching the store. |
| 2026-07-27 | #95 | #103 | The terminal gets a font size, and it is an input to the grid rather than a display tweak. The reader's only lever was the board's zoom, which resizes every other shape with it; but the font size is the divisor the grid is derived from, so a `+` that only assigned `terminal.options.fontSize` would have left `cols`/`rows` coming from a cell measured at 13px and the emulator drawing past a frame that clips rather than scrolls — bigger text, silently fewer visible columns. So `terminalGrid()` takes the font size as a defaulted second argument, and one size feeds the cell, the frame — the header, the prompt strip and the padding are all `em`, so the chrome grows with the text it holds — and therefore the grid, reported down the same debounced route a corner drag uses. Two buttons on the header with `pointer-events: auto` on the buttons alone, because every pixel that takes the pointer stops selecting the shape; buttons rather than a shortcut because while the terminal has the keyboard, `Ctrl+-` belongs to the shell. The three open questions are answered rather than deferred: the size is persisted in `localStorage` next to the theme, it is global rather than per board — a preference about the reader's eyes, not a fact about a project, and never `customData`, which is stripped at both doors — and `TERMINAL_CELL` was measured against a real render at six sizes rather than assumed. That measurement found the one thing left alone: xterm's cell *width* is linear and a shade conservative, but its *height* is the line height applied to the font's measured line box rather than to the font, so a row is nearer 1.55× than the 1.35 the constant assumes and about two rows are clipped. That is true at 13px with no button pressed, and correcting it changes what every shell is told on every board, so it is recorded in `docs/terminal.md` for its own issue. |
| 2026-07-27 | #92 | #100 | An autosync the suppression counter refused is owed, not dropped. `scheduleAutoSync` answered a raised counter by returning, which armed no timer and left nothing to arm one later, so a change whose `onChange` relayouts waited for a *later* change that happened not to — which is why a block dropped by `+` could still be browser-only when the research run was clicked, and why the failure was intermittent for a person and unreproducible from a script. Measured rather than reasoned: the check records every `/api/elements/sync` and every `setTimeout(…, 1200)`, and 1200 is the debounce and nothing else, so an arm is a schedule and its absence is a refusal. Against the old code there is no arm at all in the six seconds after the click, while an arrow key moving the same block arms and syncs as always — same block, same debounce, opposite outcome. Of the issue's two candidate fixes the general one was taken: the refusal is remembered and honoured when the counter reaches zero, through the one function every release goes through. Flushing before `POST /api/issue-block/:id` was refused as hiding a general loss behind one route. What remains open is the debounce itself — a click inside 1.2 s of the last keystroke still races the write, which is a window, not a change lost for good. |
| 2026-07-27 | #93 | #98 | Erasing the terminal block undoes itself, and Alt+T is the one way back from all three ways it can be absent. The block is not locked and will not be: `locked` is the only thing Excalidraw's eraser respects, and it is also what gives the block its selection, its drag and its corner resize — the interface, not a detail. So the erase is undone rather than refused, and only while the shell is alive; once it has exited the block is a notice, and a notice the reader clears stays cleared. It comes back where it was rather than re-anchored to the right of the board, because a restore that also moved it would answer an accidental erase with a second one. The key stops standing down when no session is open — it POSTs one, which was inert in exactly the two cases a reader reaches for it — and it now says so on the block, where the shell's exit is already drawn. Found while building it, and the reason the restore looked like it un-erased the whole canvas: `convertToExcalidrawElements` rebuilds an element from a skeleton and has no `isDeleted` to rebuild from, so `ensureTerminalBlock` handing it the tombstones was resurrecting everything the eraser had just taken. It reads the live scene now. Left out on purpose: nothing in the frontend calls `DELETE /api/terminal`, and a way to close a session is its own decision. |
| 2026-07-27 | #86 | #91 | The observations get a column of their own, and a researched issue is moved out of it. A hand-written block and the issue it becomes shared one column, and the header above them carried two numbers to say which was which; the server now moves the issue a research run created to Todo, so the landing column is decided here rather than by GitHub's *Item added to project* workflow and asserted by nothing. `moveIssueToInProgress` becomes `moveIssueToColumn`, taking both the column to write and the `board.config.json` key that names it, so a missing column says how to fix itself. The header goes back to one number, counting everything the column holds — drafts, cards, and the cards the cap left out — rather than the mirrored cards alone, which is why a column of drafts read `Todo (0)`. Adding `My Notes` to the project stays a maintainer's step: `updateProjectV2Field` rewrites the whole option list from an input carrying no option ids, so automating it would clear every item's Status. *(Entry written after the fact, on the #93 branch: this merge landed without one, which is the direction `scripts/check-board-map.mjs` fails on.)* |
| 2026-07-27 | #85 | #90 | The board is resolved before the socket opens, so a load costs one connection instead of two. The old order — connect on `default`, then let `GET /api/workspaces` *switch* — made every refresh pay for a second connection and a blank canvas, which is how it arrived as a bug report: a blank canvas reads as data loss. Which board comes from `?workspace=` first and `localStorage` second, both answers to the issue's open question rather than one: a board gets a URL so two can be open side by side, and a plain refresh still returns where the reader was. Switching now keeps the previous board on screen and holds autosync off by name, because autosync was what the blanking was really protecting against. The pill gets a third state — a socket that has never been up is *Connecting*, and calling that Disconnected is the complaint itself — with immediate-then-backoff retries and a `ping`/`pong` heartbeat, since a browser cannot send a protocol ping and a half-open socket otherwise reads Connected until TCP gives up. `GET /api/files` is scoped by reference rather than by ownership: the store stays shared because a file is content-addressed and two boards may legitimately point at the same one. |
| 2026-07-27 | #84 | #89 | The board is cut into two marked sections, each with a key: **Project structure** (`Alt+P`) and **Development** (`Alt+G`). The key lives on the shape rather than in `App.tsx`, so a board declares its own navigation and the feature stays right for every other project — the same reasoning that keeps the implement agent's prompt free of this repository's workflow. `Alt+D` was the obvious key for Development and was not taken: it focuses Chrome's address bar on Windows, and a CDP-injected key goes straight to the renderer, so no check here could tell whether `preventDefault` suppressed it. This log replaces `delivered.md` and `open-work.md`, and `CLAUDE.md` now makes updating both halves part of finishing. |
| 2026-07-27 | #75 | #88 | The terminal gets a real PTY, and an emulator to draw it. `claude` typed into the block never entered its interface: a process spawned on three pipes sees `stdin.isTTY` false, and every full-screen program asks that first. `@lydell/node-pty` is an optional dependency with prebuilts, so a checkout without it still compiles and falls back to the pipes — the block says which of the two modes it got, because the same feature behaving differently on two machines with no way to tell is worse than either. |
| 2026-07-27 | #82 | #87 | A `+` that adds a project, and per-project agent settings. The registry was read-only in code and every agent setting was a module constant read once at startup, so adding a project meant hand-editing a JSON file outside this repository and one board ran every project's agents at the same model. The registry is read per request and written back through a temp file and a rename, so unknown keys survive rather than being re-serialised from the shape this code happens to know. |
| 2026-07-27 | #79 | #83 | A column's count is what it holds, and a draft is held: the number beside a column counts drafts too, so a block dropped by the `+` stops being invisible to the header above it. |
| 2026-07-27 | #78 | #81 | Enter finishes an observation and Shift+Enter breaks the line. Excalidraw's own label editor takes Enter as a newline, so the block needed its own answer rather than a button nobody presses. |
| 2026-07-27 | #77 | #80 | The implement agent researches too. Both agents are pointed at the project's own record for anything the repository does not settle, rather than one of them guessing from the prompt. |
| 2026-07-27 | — | #76 | The agent that was asked for is the agent that runs. Creating and finishing an issue stopped falling back to a default when a specific one had been named. |
| 2026-07-27 | #68 | #74 | An issue read once is remembered, and a mirrored card keeps the run drawn on it. The mirror redraws from GitHub on every read, so the run state is written onto the card as it is drawn rather than fetched a second later — the shape under the pointer already knows. |
| 2026-07-27 | #67 | #73 | A run says how long it has been going and what it has spent. The two instants are stored, not the duration: the clock runs in the browser, so the server writes twice rather than once a second. |
| 2026-07-27 | #69 | #72 | `draftCreatedAt` survives the round trip, asserted in a browser rather than inferred from a passing type check. |
| 2026-07-27 | #51 | #71 | The board gets a terminal on its right, running in the project. The block is derived: it exists for as long as the shell does, so it is stripped from the autosync and from the export instead of being saved. |
| 2026-07-27 | #58 | #70 | A screenshot on the clipboard can be pasted onto an issue block. Attachments are an explicit list on the block, not Excalidraw group membership — grouping for layout must not silently change what the agent sees. |
| 2026-07-27 | — | #66 | A worktree stops emptying the project's `node_modules`, and the mirror's `+` takes a click again. |
| 2026-07-27 | #64 | #65 | The implement agent is told its base can move: other implementations run at the same time, so the default branch is usually not where it was when the branch was cut. |
| 2026-07-27 | #54 | #63 | The state of the work is drawn on the board. A dashed outline is a run in flight, a solid one a run that finished — the board says what is happening without anything being opened. |
| 2026-07-27 | #53 | #61 | The issue block's action row splits: **Add observations** beside **Implement / Fix**, so adding to an issue is not the same gesture as starting work on it. |
| 2026-07-27 | #55 | #62 | Drafts stack newest-first and the column reflows as one grows, which is the order the mirror already uses for issues. |
| 2026-07-27 | #57 | #60 | The ceiling comes off the issue agent, and the block gets a way back from a run that failed. A timeout that kills a working agent leaves an issue created and a block that says it was not. |
| 2026-07-27 | #50 | #59 | Starting an implementation moves the issue to **In Progress** on the GitHub project, so the board and the project agree without anyone dragging a card. |
| 2026-07-27 | #49 | #56 | Every implementation gets its own git worktree, cut from the default branch, at `<project>-worktrees/issue-<n>`. Parallel runs in one checkout cannot each hold a branch. |
| 2026-07-27 | #46 | #52 | Reference images attach to an issue block and travel with the run. |
| 2026-07-27 | #47 | #48 | A mirrored card is a working issue block. The block and the card are drawn by different code and stored in different places, but a reader who can act on one should be able to act on the other, so the panel resolves both to the same thing. |
| 2026-07-26 | #25 | #45 | The GitHub project board is mirrored on the canvas, on the left, with two-way sync. Columns come from GitHub rather than being declared here, and the mirror is never saved — a saved column is a snapshot of someone's Todo from whenever the export ran. |
| 2026-07-26 | #43 | #44 | An implementation runs as long as it takes, and the block has a way back from a failure. |
| 2026-07-26 | #41 | #42 | An issue is implemented from the block that opened it, so the board is where work starts as well as where it is written down. |
| 2026-07-26 | #39 | #40 | The close button gets its own row and the card doubles in width. |
| 2026-07-26 | — | #38 | The block records that it holds its title. |
| 2026-07-26 | #36 | #37 | The card closes when the shape is deselected. The fix was to make the panel one function returning one answer, including "nothing": the defect was a branch that cleared the document and returned, leaving half the card pointing at a shape nobody had selected. |
| 2026-07-26 | #34 | #35 | The font size the title was laid out for is written down, so the layout is reproducible rather than re-derived. |
| 2026-07-26 | #20 | #33 | The documentation is anchored to the block, not to the window. Excalidraw's `<Sidebar>` is pinned to the window edge by CSS and `docked` only decides whether it pushes the canvas, so on a wide board a block sat thousands of pixels from its own documentation. The anchored card replaces the sidebar rather than coexisting with it — a mode switch nobody asked for is a setting to maintain forever. |
| 2026-07-26 | — | #32 | A retitled block is recorded at its corrected size. |
| 2026-07-26 | #30 | #31 | The title is laid out *inside* its block rather than merely into it. |
| 2026-07-26 | #28 | #29 | A failing `gh` is retried instead of being shown as an error: it fails intermittently here with socket-buffer exhaustion, and the first failure is usually not real. |
| 2026-07-26 | — | #27 | The issue a block produced is shown on the block. |
| 2026-07-26 | #23 | #24 | The tool is mapped on its own board, `docs/board.excalidraw`, with a card per document. |
| 2026-07-26 | #21 | #22 | Every development artifact is written in English, enforced by `scripts/check-english-only.mjs`. The convention had leaked twice — Portuguese fixtures in the check scripts, and an issue the agent wrote entirely in Portuguese after reading a repository that documented in it. The prompt now fixes the output language rather than leaving the agent to take it from what it just read. |
| 2026-07-26 | — | #19 | The issue block works end to end against a real repository. |
| 2026-07-26 | — | #18 | The observation is read from the shape's label, which is where it was already being written. |
| 2026-07-26 | #11 | #17 | The issue block: an observation written on the board becomes a researched GitHub issue. |
| 2026-07-26 | #10 | #16 | The collapsible image block. `customData.collapsed`, with `fullSize` stashed before shrinking so expanding is not a guess. |
| 2026-07-26 | #9 | #15 | `GET /api/library` serves the environment `.excalidrawlib` plus the project's own, so the blocks that make a board work are on every board. |
| 2026-07-26 | #8 | #14 | Board tabs, and a docs panel that knows which workspace it is in. |
| 2026-07-25 | — | #13 | Element storage is scoped to a workspace: `elements` stopped being one global `Map` and every route resolves its store from `?workspace=`. |
| 2026-07-25 | #7 | #12 | A workspace registry, so a board can belong to a project. One `board.config.json` per project, and the Windows and WSL spellings of a path collapse onto one key. |
| 2026-07-25 | #3 | #6 | The documentation panel: a shape carrying `customData.docKey` opens the markdown behind it from `GET /api/docs/:key`. This is the mechanism the whole board is built on. |
| 2026-07-25 | #2 | #5 | `link` and `customData` are accepted by the create and update schemas. Both were dropped there but survived a sync — an asymmetry that stopped an agent from binding a shape to its documentation through the API at all. |
| 2026-07-25 | #1 | #4 | `POST /api/elements/sync` reconciles by version instead of clearing and rewriting. Absence used to mean deletion, so anything created through the API vanished on the next autosync. The merge is by `id`, highest `version` wins, `versionNonce` breaks the tie, and a deletion travels as an explicit tombstone. |

## The pattern under the first eleven

Three of the first eleven issues were the same shape of bug: two writers to one store, with no
rule for what happens when they disagree. Clear-and-replace sync (#1), schemas that dropped
fields another path accepted (#2), and a global `Map` shared by every board (#8). The fix each
time was to make the rule explicit rather than to make one writer back off.

That is still the rule the newer half of this log follows. The mirror and the terminal are the
same problem read the other way: they have exactly one writer, so they are marked *derived* and
never stored at all.

## The habit that keeps costing

Every behaviour change here ships with a `scripts/check-*.mjs`, run against the old code first.
The ordering is not ceremony. Three defects in the UI layer — a panel that never opened, a race
in tab startup, a click landing on the label instead of the box — compiled, type-checked, and
did none of what they claimed. Nothing short of driving a real browser would have caught any of
them, which is why the browser checks exist and why `CLAUDE.md` says compiling is not working.
